{{ form_start(form,{attr : {class : 'filter js-filter-form'}}) }}
<div class="spinner-border js-loading" role="status" aria-hidden="true" style="display: none">
    <span class=sr-only"></span>
</div>
                {{ form_row(form.q) }}

<hr>
<h4>Couleurs</h4>
{{ form_row(form.couleurs) }}
<hr>
<h4>Tailles</h4>
{{ form_row(form.tailles) }}
<hr>
<h4>Prix</h4>
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.min, {'attr' : { 'id' : 'min' , 'value' : min}}) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.max, {'attr' : { 'id' : 'max' , 'value' : max}}) }}
    </div>
</div>

<div id="prices-slider" data-min="{{ min }}" data-max="{{ max }}" style="margin-bottom: 3rem">

</div>
{{ form_end(form) }}